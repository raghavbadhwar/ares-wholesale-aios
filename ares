#!/usr/bin/env python3
"""
Ares Agent CLI launcher.

This wrapper should behave like the installed `ares` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from ares_cli.main import main
    main()
